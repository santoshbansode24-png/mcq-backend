-- Mental Math Feature Cleanup
-- Run this in phpMyAdmin to clear all Mental Math data

-- Drop the mental_math_progress table if it exists
DROP TABLE IF EXISTS `mental_math_progress`;

-- You can recreate it from scratch when developing the new feature
-- For now, the table is completely removed

SELECT 'Mental Math feature cleared successfully!' as status;
