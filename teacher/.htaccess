<IfModule mod_rewrite.c>
  RewriteEngine On
  # Route everything to index.html if it's not a real file or directory
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
